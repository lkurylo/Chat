var FPChat = FPChat || {};
(function ()
{
    //contains options for the FPChat
    FPChat.options = (function ()
    {
        return {
            'refreshUserListTimeout': 15000,
            'refreshMessagesBoxTimeout': 2000
        };
    })();
})();